# Pinned Flutter SDK version for the local release path (ADR-0009) and CI —
# read directly by both CI workflows with the same parse the F-Droid recipe
# uses. On a bump, no fdroiddata edit is needed: the F-Droid recipe parses
# this file from the tagged commit
# (metadata/io.github.benediktburger.cycleapp.yml).
3.47.4
